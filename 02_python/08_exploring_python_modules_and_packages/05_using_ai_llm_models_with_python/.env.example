# Copy this file to ".env" (without .example) and add the real key there.
# Never commit the actual ".env" file to GitHub — it is already listed in .gitignore.

GEMINI_API_KEY=your_real_api_key_goes_here
